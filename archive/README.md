# Archive

Reference material kept out of the active V3 app tree on `swag_V3_new_branch`.

| Path | Role |
|---|---|
| `qa/` | Visual QA screenshots (wordmark dock, reduced motion, etc.) |
| `v2/` | Pre-V3 section + motion components and `copy.v2.ts` (live on `main`) |
| `stitch-compositions/` | Expanded Stitch HTML/PNG pack + zip |

Do not import `archive/v2` from `app/` or `components/v3/`. Production V2 remains on `main`.
